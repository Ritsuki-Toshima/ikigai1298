<div class="container">
  <% user = User.find(@appointment.user_id) %>
  <%# <div class="text-center mt-5">
    <div class="d-flex justify-content-center align-items-center">
      <i class="fa-regular fa-clock" style="font-size: 36px;"></i>
      <%# <h2 style="font-size: 50px;"><%= Time.now.in_time_zone.strftime("%l:%M") <span style="font-size: 24px;"><%= Time.now.in_time_zone.strftime("%p")</span></h2> %>
  <%# </div> %>
  <%# <p><%= Time.now.in_time_zone.strftime("%A %d %B") %>
  <%# </div> %>
  <div class="card">
    <h3 class="card-header">
      <%=@appointment.name.split.map(&:capitalize).join(' ') %>
    </h3>
    <div class="card-body">
      <h2 class="card-title"><%="#{@appointment.start_time.strftime("%b %e, %l:%M %p")}" %></h2>
      <h3 class="card-text"><%= "#{@appointment.description}" %></h3>
      <h3 class="card-text">Location: <%= "#{@appointment.address}" %></h3>
      <a class="btn btn-primary mb-3" data-bs-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">
        View on Map
      </a>
      <div class="collapse" id="collapseExample">
        <div style="width: 100%; height: 360px;"
          data-controller="map"
          data-map-marker-value="<%= @marker.to_json %>"
          data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
        </div>
      </div>
    </div>
  </div>
</div>
